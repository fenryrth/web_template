# GitHub Copilot adapter

Treat `AGENTS.md` at the repository root as the canonical agent instruction entrypoint.

Before substantial changes, read `AGENTS.md`, `PROJECT.md`, `HANDOFF.md`, and the relevant canonical documents referenced by `AGENTS.md`.

Do not duplicate project state or durable project rules in this adapter.
